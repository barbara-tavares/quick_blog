 <p id="notice"><%= notice %></p>

 <%= render :partial => @post %>

 <%= link_to 'Edit', edit_post_path(@post) %> |
 <%= link_to 'Back', posts_path %>

<h2>Comments</h2>
 <div id="comments">
         <%= render partial: 'comment' %>
 </div>

 <%= simple_form_for [@post, Comment.new] do |f| %>

    <%= f.input :body, label: 'New comment' %> </br>
    <%= f.button :submit%>

<% end %>